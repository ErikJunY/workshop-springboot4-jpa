# ==== MYSQL CONFIG ====
spring.datasource.url=jdbc:mysql://localhost:3306/springboot_course?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Dacco0202!

# Driver opcional (Spring geralmente detecta sozinho)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==== JPA / HIBERNATE ====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto correto para MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ==== JWT ====
jwt.secret=MYJWTSECRET
jwt.expiration=3600000
